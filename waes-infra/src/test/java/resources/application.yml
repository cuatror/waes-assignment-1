spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:demo
    dbcp2:
      driver-class-name: org.h2.Driver
    username: user
    password:
  jpa:
    hibernate:
      ddl-auto: create